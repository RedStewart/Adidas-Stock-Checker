import React from 'react';

const Search = () => {
  return (
    <section id='searchbar'>
      <input type='text' />
      <input type='text' />
      <button>Search</button>
    </section>
  );
};

export default Search;
